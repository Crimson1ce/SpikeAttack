Run the executable `run_game` to compile and run the code.

The game doesn't accept user input: the direction the player moves in is hardcoded in this version.

This game was developed in order to be implemented on a [REDACTED] board that allowed the user to move freely.
